import React from "react";

/**
 * Creates a footer, which belongs at the bottom of every page.
 * 
 * @returns {React.ReactNode} Footer.
 */
export default function Footer() {
  return <footer className="page-footer"></footer>;
}
